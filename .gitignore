node_modules/
.next/
out/
build/
dist/

# Every dotenv variant is secret; .env.example is the only one tracked.
# `vercel env pull` writes files like .env.local and .env.production.local.
.env*
!.env.example

# Local photo storage driver output
.uploads/

*.tsbuildinfo
next-env.d.ts

.DS_Store
npm-debug.log*

# Local database snapshots from `npm run db:backup`
backups/
